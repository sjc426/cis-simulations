(function(){function animateBanner(){var track=document.getElementById('cis150-track');if(!track){setTimeout(animateBanner,300);return;}var pos=0,speed=0.4,totalWidth=track.scrollWidth/2;function step(){pos+=speed;if(pos>=totalWidth)pos=0;track.style.transform='translateX(-'+pos+'px)';requestAnimationFrame(step);}requestAnimationFrame(step);var circles=document.querySelectorAll('#cis150-net circle');circles.forEach(function(c,i){var r=parseFloat(c.getAttribute('r'))||4,delay=i*300;function pulse(){var s=1+0.5*Math.abs(Math.sin((Date.now()-delay)/1000));c.setAttribute('r',r*s);c.style.opacity=0.5+0.5*Math.abs(Math.sin((Date.now()-delay)/1000));requestAnimationFrame(pulse);}setTimeout(function(){requestAnimationFrame(pulse);},delay);});}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',animateBanner);}else{animateBanner();}})();